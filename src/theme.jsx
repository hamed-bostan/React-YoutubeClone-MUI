import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        primary: {
            main: '#0f0f0f',
            light: '#fff',
        },
        secondary: {
            main: 'rgba(0,0,0,0.1)',
            light: 'rgba(0,0,0,0.05)',
        }
    }
})
